import 'package:flutter/material.dart';

class GoOut extends StatelessWidget {
  const GoOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Go Out Page');
  }
}
